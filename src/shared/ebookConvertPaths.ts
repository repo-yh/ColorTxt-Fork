/** 位于 `app.getPath("userData")` 下的默认电子书转 txt 缓存子目录名 */
export const EBOOK_CONVERT_DEFAULT_SUBDIR = "ConvertedTxt";

/** 彩读书包解压目录（无同名列表项时） */
export const UNPACKED_BOOKS_DEFAULT_SUBDIR = "UnpackedBooks";
