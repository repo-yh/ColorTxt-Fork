## 3.6

新功能：

- **词典查词**
  - 选区工具条增加「词典」按钮（默认不显示，可在「设置 → 阅读 → 工具条」开启）
  - 点击后在选区旁弹出查词浮层，按已启用词典顺序展示结果
  - 「词典管理」：内置 Wiktionary、Wikipedia 网络词典；可导入本地词库（StarDict / MDict / DICT / Slob / BGL）

改进：

- 网络导入书源增加重试逻辑并显示 loading
- 主界面「设置」增加「代理」标签页（和找书窗口共用一套配置），以支持「网络词典」等需要发送网络请求的场景

修复：

- 「配色 → 高亮色/标注色」现在可以正常删除

## 3.5

新功能：

- 侧栏「文件」列表增加「**树形模式**」 [#52](https://github.com/ssnangua/ColorTxt/issues/52)
  - 标题旁可切换 **列表 / 树状**（默认列表）
  - 树按路径去掉公共父目录展示，文件夹右侧显示「（文件数，总大小）」
  - 点击文件夹「展开 / 折叠」，工具栏「分类」左侧可「全部展开 / 全部折叠」
  - 文件夹右键菜单支持批量「分类 / 移除」或「在文件管理器中打开」
- 「高亮词」功能增强
  - 支持 **AI 检索** 语义（如 `人名`/`地名`），**批量添加** 高亮词 [#56](https://github.com/ssnangua/ColorTxt/issues/56)
  - 支持 **手动输入** 添加高亮词，支持 **编辑** 已添加的高亮词
  - 支持 **单项多词**，比如一个高亮词项可以包含 `东邪`/`西毒`/`南帝`/`北丐`/`中神通`，在列表中会展示为 `东邪 (+4)`，点击 `+4` 展开
    - 可以单独点击 `东邪` 来跳转到下个 `东邪`，也可以点击整项进行多词跳转（正则匹配）
    - 可以 **拖动整项** 到另一个高亮词项上进行 **合并**，也可以 **拖动单个词**：内部拖动 → **调整顺序**；拖动到外面 → **拆分**；拖动到其他高亮词项 → **移动**

改进：

- 阅读器工具条
  - 增加「查找」按钮（默认不显示）
  - 「设置 → 阅读 → 工具条」可配置「复制」「查找」「问 AI」是否显示；「查找」可设置应用目标为「查找栏 / 全文搜索」（默认「查找栏」）
  - 划线后不再显示「移除划线」按钮，而是和高亮词一样在色盘最前面显示「移除」图标
  - 工具条优先按阅读区钳制，右侧会被窗口裁切时再按窗口钳制（可伸入侧栏）
- 侧栏「文件」分类非「全部」时，再次添加已在列表中的文件会改到当前分类（含「未分类」）
- 完善书源解析逻辑

修复：

- 「AI 阅读助手」思维导图现在只渲染一次 [#57](https://github.com/ssnangua/ColorTxt/issues/57)
- 内容上色「引号内文字」现支持全角双引号 `＂`（U+FF02）

## 3.4

> [!IMPORTANT]
> 重要体验优化版本，建议更新

- 「设置 → 阅读」新增以下配置
  - 「**启用中文换行优化**」（默认开启）：给 Monaco 打补丁，在**简单换行策略**下将中文标点按全角估算、用汉字校准全角字宽，减轻行尾缺字与溢出；不作用于**高级换行策略**
  - 「**段间距**」（`0～100`，默认 `0`）：给 Monaco 打补丁，每个**物理行**（自动换行前的一行）结束后增加**常数像素空隙**；同一物理行因软换行产生的多行之间不加距（区别于**行间距** `lineHeight`，额外状态 O(1)） [#53](https://github.com/ssnangua/ColorTxt/issues/53)
  - 「**字间距**」（`-5～20`，默认 `0`）：Monaco 原生 `letterSpacing`
  - 「**左右边距**」（`0～160`，默认 `0`）：收窄编辑器区域形成正文左右留白；阅读区变窄时自动压缩留白；留白区滚轮可滚动正文 [#35](https://github.com/ssnangua/ColorTxt/issues/35)
  - 「压缩空行 → **章节标题上下保留空行**」：仅在开启「压缩空行」时生效；可选「上面 1 空行」「上面 1 空行，下面 1 空行」「上面 2 空行，下面 1 空行」（默认）
- 设置 / 顶栏 / 快捷键：原「行高」文案改为「**行间距**」（仍为 `lineHeight` 倍数，与「段间距」不同）
- 阅读模式右键：**编辑选中文本**（≤1000 字局部修改原文）
  - 增加快捷键 `Ctrl + /` 切换编辑模式，`Ctrl + E` 编辑选中文本
- 编辑原文保存、清除失效文件等耗时操作显示 loading 蒙层
- Windows NSIS：安装/更新改为**覆盖安装**（不清空目录）；**完整卸载仍会删整个安装目录**

## 3.3

改进：

- 原生可编辑控件（输入框 / 文本域）支持右键菜单（剪切 / 复制 / 粘贴等）
- 阅读器只读模式支持右键菜单「复制 / 全选」
- 找书窗口
  - 下载书籍时，如果下载目录存在同名文件，直接覆盖（主界面文件列表不会同时存在同一本书的多个副本，方便更新内容）
  - 「书籍信息」元数据区支持右键菜单「复制 / 全选」
- AI 智能排版
  - 明确禁止结构助词「的/地/得」的语法互替，减轻 Diff 噪声 [#44](https://github.com/ssnangua/ColorTxt/issues/44)
  - Diff 预览支持 `Ctrl+F` 查找；点「放弃」时增加确认弹框，避免误操作
- 编辑替换规则：
  - 「替换规则 / 替换为」支持内联高亮与单字段全屏 Monaco
  - 「替换范围 / 排除范围」聚焦时显示输入建议，选择后追加到末尾
    - 找书窗口输入建议：当前书名 / 当前书源
    - 主界面输入建议：当前书名（有打开文件时）

修复：

- 电子书转 Markdown：图片/链接目标路径中的 `(` `)` `\` 等按 CommonMark 转义写出；解析侧走 marked、不再做非标准兜底（**旧 md 需重新转换**）
- 找书窗口
  - 书源网络导入功能增强，之前 Node/undici 的 TLS 会被部分站点（或前面的 Tengine）重置，现在能正常导入
  - 阅读器进入编辑模式时，现在会恢复视口锚点（不会乱跳）

## 3.2

新功能：

- **彩读书包**
  - 底栏路径菜单可「**导出书包**」（`txt`/`md`/`插图目录` + `书签`/`高亮词`/`笔记`/`角色卡` + `阅读进度`）
  - 书包默认扩展名 **`.ctz`**（普通 zip 包）/ **`.ctzx`**（加密包）
    - 安装「彩读」时，安装程序会将「彩读」注册为 `.ctz`/`.ctzx` 的打开方式，可双击打开
    - `.txt`/`.md` 仅加入「打开方式」，不抢系统默认
  - 「打开文件」可 **打开书包**，「选择目录」可 **导入书包**
    - 拖放操作同样支持打开/导入书包文件
    - 按文件名匹配「当前打开/最近打开/文件列表」，_更新_ 或 _解压到「书包解压目录」_
  - 「设置 → 常规」增加配置项「书包解压目录」「**书包默认密码**」
- **WebDAV 同步** [#5](https://github.com/ssnangua/ColorTxt/issues/5)
  - 「设置 → WebDAV」：启用、服务器地址 / 用户名 / 密码 / 远端目录
  - 主界面：
    - 侧栏「WebDAV」面板：上传/同步「配置」；拉取上传的书包列表，可过滤、排序、下载
    - 底栏「文件路径」菜单：上传/同步「书包」（基于当前打开文件导出的带阅读进度的书包）
  - 找书窗口：
    - 顶栏「WebDAV」菜单可分别上传/同步「书架 / 书源 / 设置」
- 语音朗读：Windows 新增服务商「**讲述人自然语音**」（通过 Windows SAPI5 调用本机音色，需安装适配器与语音包，如 [NaturalVoiceSAPIAdapter](https://github.com/gexgd0419/NaturalVoiceSAPIAdapter)） [#36](https://github.com/ssnangua/ColorTxt/issues/36)
- 「设置 → 常规 → 数据管理」：新增「**阅读数据**」面板，可管理各书阅读数据

改进：

- 语音朗读
  - 小米 MiMo 按 API 密钥前缀自动选择网关（`sk-` 走按量付费，`tp-` 走 Token Plan 中国集群） [#41](https://github.com/ssnangua/ColorTxt/issues/41)
  - AI 识别说话人时会带上少量上下文，能一定程度上增加一些准确性 [#39](https://github.com/ssnangua/ColorTxt/issues/39)
- 主界面
  - 侧栏「文件」面板增加「更多」→「选择文件 / 移除失效文件」
  - 侧栏「文件」列表里的 `.txt`/`.md` 文件右键菜单可「替换文件」 [#38](https://github.com/ssnangua/ColorTxt/issues/38)
  - 侧栏「章节」面板增加「更多」→「复制目录」
  - 侧栏「搜索」面板增加「更多」→「复制 / 复制（带行列号）」
  - 侧栏「书签」面板增加「更多」→「**导出书签** / **导入书签**」
  - 侧栏「角色卡」面板增加「更多」→「**导出角色卡包** / **导入角色卡包**」
- 找书窗口
  - 顶栏「更多」新增菜单项「新窗口 / 快捷键 / 配色 / 主界面 / 检查更新 / GitHub / 关于」
  - 阅读器工具栏「更多」新增菜单项「查找 / 配色」
- 「设置 → 阅读」新增「滚动」→「**滚动倍率** / **滚动加速倍率**（按住 `Alt`）」
- 找书窗口与主界面共用「阅读 / 编辑 / 语音朗读」设置（各窗可单独设置，只在打开时读最后保存值，不做运行时互相同步）
- 找书窗口与主界面共用「配色 → 阅读器」设置（和「主题色」一样会实时同步）
- 统一导出文件名：

| 模块    | 文件名                                                   |
| ------- | -------------------------------------------------------- |
| 书签    | `{书名}.bookmarks.json`                                  |
| 高亮词  | `{书名}.highlights.json` / `favorite.highlights.json` |
| 笔记    | `{书名}.notes.{json\|md}`                                |
| 角色卡  | `{书名}.characters.zip`                                  |
| AI 对话 | `{书名}-{对话标题}[（带思考过程）].chat.{json\|md}`      |

修复：

- 多主窗口时拖拽调整侧栏宽度不再实时同步到其它窗口

## 3.1

新功能：

- **番茄时钟**（保护眼睛，健康阅读，从你我做起！）
  - 「设置 → 阅读」新增「番茄时钟」配置（默认开启），可设置「阅读时长」「短休息时长」「长休息时长」
  - 启用「番茄时钟」时，会在底栏左侧显示「<img src="./src/renderer/src/assets/history.svg" width="18" height="18" alt=""> 开始番茄时钟」按钮，开始后显示为「饼图」倒计时，点击「饼图」会展开为「饼图 + 倒计时 + 暂停/继续 + 停止」
  - 「阅读时长」倒计时结束时，整个界面会呈现 **毛玻璃** 效果，并开始「休息」倒计时
  - 休息结束后，需要重新点「开始番茄时钟」
  - 每完成 4 轮「阅读时长」会进入一次「长休息」（「停止」番茄时钟不会重置「长休息」计数）

改进：

- 「设置 → 阅读」新增「全屏时在左下角显示系统时间」配置项（默认开启）
- 找书窗口阅读器面板侧边栏开关状态持久化；打开阅读器时，如果书籍只有一个章节，默认关闭侧边栏
- 「高亮词」支持导入/导出 [#30](https://github.com/ssnangua/ColorTxt/issues/30)
- 「书架」增加 **分类** 功能；书籍卡片功能增强（书名 → 书籍信息，作者 → 搜索作者，书源 → 书源搜索，分类 → 筛选分类，「更多」菜单增加「搜索书名」菜单项）
- 「**编辑书源**」体验优化：内联高亮 + 单字段全屏编辑器（内置 API 智能提示）
- **完善书源解析逻辑**

修复：

- MiniMax 文生图 API 实际生成时多拼接了一层 `/v1` [#32](https://github.com/ssnangua/ColorTxt/issues/32)
- 修复书架直开阅读器时，加载章节正文失败的问题
- 通过「右键 → 打开方式 → 彩读」打开 txt 时，可能会误路由到找书窗口且无反应；现始终在主阅读窗口加载
- 安装后「打开方式」支持关联 `.md`（与 `.txt` 一样可直接用彩读打开）

## 3.0

新功能：

- **找书**（支持 `阅读（Legado）` 文本书源）
  - 入口：主界面「更多 → 找书」（快捷键：`F7`）
  - 界面：「书架」「找书」「发现」
  - 书源：找书窗口「更多 → 书源管理」打开面板，可导入、导出、编辑书源
  - 功能：搜索书籍、在线阅读、整书下载
  - 设置：找书窗口「更多 → 设置」可配置「下载」「代理」等相关选项

> [!NOTE]
> `阅读` 书源用到了一些 `Java` 接口，彩读移植了一套 `JavaScript` 实现，做不到 100% 复刻，具体见 [§ 规则能力与兼容范围](https://github.com/ssnangua/ColorTxt/blob/main/docs/%E4%B9%A6%E6%BA%90%E6%89%BE%E4%B9%A6.md#7-%E8%A7%84%E5%88%99%E8%83%BD%E5%8A%9B%E4%B8%8E%E5%85%BC%E5%AE%B9%E8%8C%83%E5%9B%B4)，大概率有错漏，只能通过迭代不断纠错兼容，目前算是测试版，如发现问题，请提交 [issue](https://github.com/ssnangua/ColorTxt/issues/new)

> [!WARNING]
> 「彩读」**不提供、不内置、不分发任何书源**，鼓励大家通过正规渠道支持作者与正版，具体见「更多 → 免责声明」。

- **文本替换**：全局替换文本，可用于替换人名、去广告文本等
- **章节导航工具栏**：可在「设置 → 阅读」中启用，会在阅读区底部显示「上一章 / 下一章」快捷跳转

改进：

- 「设置 → 常规」增加「章节字数显示具体数值」配置项，开启后章节列表字数显示为具体数值（如 `23,123 字`）[#27](https://github.com/ssnangua/ColorTxt/issues/27)
- 「设置」重置当前页时，_缓存目录_ 类配置项的重置行为统一为 _填入默认值_

修复：

- 多窗口下阅读进度可能被后关闭的窗口用旧进度覆盖 [#25](https://github.com/ssnangua/ColorTxt/issues/25)
- 「设置 → 向量模型」设置为「内置本地模型」后如果 _未下载_ 模型，关闭「AI 阅读助手」会无法保存 [#21](https://github.com/ssnangua/ColorTxt/issues/21)
- 词云弹框字体列表层级问题
- macOS Intel 包因交叉编译缺少 `@node-rs/jieba-darwin-x64` 导致打开即崩溃；现在改成 `macos-15-intel` 打 x64
- macOS Intel 包在 arm64 CI 交叉构建时打入 arm64 的 `opencc.node`（`have 'arm64', need 'x86_64'`）；改为 `macos-15-intel` 原生构建并校验 Mach-O 架构
- Linux AppImage 在 Ubuntu 24.04 等缺 FUSE2 的系统上无法启动（`libfuse.so.2`）；改为静态 AppImage runtime [#28](https://github.com/ssnangua/ColorTxt/issues/28)

## 2.8

新功能：

- 支持「定时滚动」功能
  - 工具栏增加「定时滚动」开关
  - 「设置 → 阅读」增加「定时滚动」配置
- 「语音朗读」接入 **小米 MiMo**，支持 **音色定制**（VoiceDesign）和 **音色克隆**（VoiceClone），目前限免，可以薅一波
- 「对话模型」接入 **小米 MiMo**

改进：

- 「设置 → 阅读」增加「启用粘性章节标题」开关 [#23](https://github.com/ssnangua/ColorTxt/issues/23)
- 「语音朗读 → AI 识别」增加「情绪标注」开关，如果感觉情绪听感不自然，可关闭该设置
- 阅读器配色选项支持开关（比如阅读英文书籍时，可暂时关掉「字母」上色）[#19](https://github.com/ssnangua/ColorTxt/issues/19)
- 朗读工具条「音调」控制改为「音量」控制（设置中的「音调」配置依然保留）
- 窗口的最小尺寸调整为 650（当窗口小于一定宽度时，自动将工具栏中的「字体按钮组」/「格式化按钮组」移入「更多」菜单） [#14](https://github.com/ssnangua/ColorTxt/issues/14)
- 查找栏优化：按钮的 tooltip 强制往下弹，不会被工具栏挡住；输入框里按 `↑`/`↓` 方向键可以正常查看历史记录

修复：

- 在语音朗读设置页试听时，切换到其他设置页或关闭设置面板，现在会停止试听音频
- 语音朗读状态下，之前会禁用所有快捷键，现在只禁用阅读器滚动/跳转相关的快捷键
- 修复偶现 API 密钥丢失的问题

## 2.7

新功能：

- 「语音朗读」支持 **多音色**
  - 设置新增「朗读方案」选项：可选「单音色」或「旁白/对白多音色」
  - 启用「AI 阅读助手」时，可启用「AI 识别」，朗读时 AI 会自动识别说话人的性别，并使用相应的「男声」或「女声」音色；可在「角色卡」中为每个角色单独设置音色，朗读时 AI 会自动识别说话人，并套用相应角色的专属音色
- 「语音朗读」支持配置多套方案
- 「语音朗读」Qwen3-TTS 支持 `qwen3-tts-instruct-flash` 模型
- 「对话模型」/「文生图 API」/「语音朗读」接入 **MiniMax**

修复：

- 查找栏不再触发工具条

## 2.6

新功能：

- 工具栏新增 **转换** 按钮：支持 **简繁互转**、**字母数字全半角互转**
- **划线标注** 与 **记笔记**
  - 阅读器内选中文本后会弹出工具条，支持复制、设置高亮词、划线（马克笔/波浪线/直线）、记笔记、问 AI
  - 侧栏新增「笔记」标签页，可浏览书中所有的标注和笔记，支持导出为 `.md` 文件

改进：

- 「设置 → AI 阅读助手 → 对话模型」
  - 新增 **工具调用轮数** 配置项：单次提问内模型调用工具的最大往返次数，复杂问题可适当调高
  - 新增 **附加系统提示词** 配置项：分析敏感题材小说时，可一定程度上减少拒答
- 「设置 → 向量模型 → 模型来源：远程 API」：新增 **单次嵌入条数** 配置项
- 「角色卡」：增加 **别名** 信息（不需要手动输入，检索时会自动检索角色别名）
- 「角色卡 → 角色编辑面板 → 立绘」：支持拖放图片设置立绘
- 「对话模型」和「文生图 API」服务商接入 **Agnes AI**

修复：

- 禁用 Monaco 的大文件优化，避免大文件无法自动换行和显示粘性标题
- 章节标题里有链接或多个空格时，现在可以正确渲染 [#16](https://github.com/ssnangua/ColorTxt/issues/16)
- 避免发给模型的历史工具链不完整
- 修复 `.md` 文件的链接解析问题
- 修复 `.epub` 的目录解析问题
- 修复多级目录的跳转锚定问题

## 2.5

新功能：

- 「对话模型」/「文生图 API」支持配置多套方案，可随时切换不用重新配置
- 打开其他电子书格式不再转换为 `.txt`，而是转换为 Markdown（`.md`）
  - 电子书目录会映射为相应层级的 ATX `#` 标题
  - 内链/注脚：支持鼠标移上显示内容、跳转与回跳
  - 外链：点击打开系统默认浏览器
  - 底栏文件路径弹出菜单：新增「重新转换」选项
  - 以前转换的带有 `<<A>>`/`<<ID>>`/`<<IMG>>` 等特殊标记的 `.txt` 文件会被当成普通的 `纯文本` 文件，**请重新转换**
- 「编辑模式 → **AI 智能排版**」（需启用「AI 阅读助手」）
  - 可点击工具栏「AI 智能排版」一键全文排版，也可以在编辑器中选中文本后「右键 → AI 智能排版：选中文本」进行局部排版（长文建议分次排版）
  - 排版完成后会显示 **Diff 预览**，可对排版结果逐一确认，点「应用」后再一次性写回，「放弃」则主文档不变
    - **不同模型的排版结果可能差异较大**，_请自行测试_
  - 可在「设置 → 编辑」中自定义排版选项：
    - **清理 HTML 残留**：解码 `&#35498;` 与 `&nbsp;` 等 HTML 实体，并去除 `<br/>` 等 HTML 残留。
    - **修正硬换行**：将同一自然段内因排版产生的句中强行换行合并为一行。
    - **修正标点符号**：引号/括号配对、半角转全角、断句补标点等；不改动数字小数点与英文对白中的半角标点。
    - **统一对话符号**：将对话统一为 `“”` 或 `「」` 包裹。
    - **修正乱码**：尝试将 `锟斤拷` `�` 等乱码还原为合理汉字；_有一定误还原风险，请自行检查。_
    - **还原 \* 屏蔽**：尝试根据上下文还原正文里的 `*` 和谐字；不处理整行分隔线（如 `***********`）；_可能误猜，请自行检查。_
    - **移除盗版水印**：删除句中插入的防盗版杂符（如 `月*漪〇/泣②sa/`）；_有一定误删风险，请自行检查。_
    - **移除广告/引流信息**：删除 `发布于 www.xxx.com` `看小说，就来 xxx 网` 等站宣水印；_有一定误删风险，请自行检查。_
    - **压缩空行**：自动应用「格式化：压缩空行」。
    - **行首缩进**：自动应用「格式化：行首缩进」。
  - 可通过「设置 → 技能 → 智能排版」自定义 AI 排版行为。

改进：

- 字体列表：支持将其他字体**固定**到外层列表
- 信息栏：编辑模式下显示光标位置和选中字符数

修复：

- 章节匹配规则变更，标题行会正确格式化
- 更新角色立绘，角色卡能正常同步

构建相关：

- 改用 CI 构建，支持输出 macOS(x64)/Linux(x64) 包

## 2.4

新功能：

- **AI 阅读助手**
  - 支持输出 **思维导图**：内容概括/人物关系类问题自动出图（设置项可关）；支持 **导出 SVG**
  - 支持输出 **词云图**：可生成人名词云/地名词云等；支持 **导出 PNG**
- **角色卡**
  - 文生图：支持 **云端接口**（OpenAI Images、通义万相等）
  - 角色卡片：增加 **3D 倾斜与闪卡纹理** 效果，闪卡纹理可在「更多 → 卡片效果」里切换
  - 查看大图：改为卡片 **旋转放大**（放大后仍可翻转正反面）
  - 角色立绘生成：改为使用 **自然语言** 描述 _画风_ 和 _角色形象_，使用更简单直观
- **高亮词**：可「收藏」为 **通用高亮词**（所有书籍都会显示）

改进：

- 优化列表排序交互：上移下移的操作有点呆，改成拖动排序
- 规范文件编码显示

修复：

- macOS 现在可以正常退出应用

## 2.3

新功能：

- 「对话模型」和「向量模型」支持 **主流 AI 服务商**（DeepSeek 等）
- 「向量模型」 支持 **内置本地模型**：下载模型到本地运行，无需 API，使用 **更方便**

改进：

- 支持设置「AI 数据缓存目录」和「内置本地模型缓存目录」
- 「API 密钥」改为由 **系统钥匙串/凭据管理器** 加密保存，不再以~~明文~~的方式写入配置文件，信息 **更安全**
- 「Token 消耗量」支持开关，并增加 `命中缓存的输入 Token 数` 和 `总花费` 信息：总花费需要在「设置」中填写 Token 价格
- 「深度思考」适配主流 AI 服务商接口规范
- 「角色卡」的「AI 检索」也会输出 Token 消耗量信息了

## 2.2

新功能：

- 支持打开 **Markdown（`.md`）** 文件：与 `.txt` 文件一样直接打开（不转换）；章节按 ATX `#` 标题识别，侧栏按标题层级缩进
- **编辑模式** 支持显示 **小地图**：「设置」→「编辑」可开启小地图；小地图支持显示大号章节名，便于在长文中定位章节

改进：

- **只读 ↔ 编辑视口恢复**：进入/退出编辑时以第二行视觉行为锚点进行滚动位置恢复，减轻切换后排版差异导致的跳动
- 编辑模式支持 **自动刷新章节**：内容变更时会同步更新章节列表，不需要手动「刷新章节」了（_出于性能考虑，内容大于 30 万行时，会禁用自动刷新，仍需手动刷新_）
- **AI 阅读助手 `ragContext`**：优先从阅读器取章节原文而不是通过向量索引拼接 chunk；≤1 万字返回完整正文；超过 1 万字时按每 1 万字一段压缩为约 1 万字全章提要；压缩过程中显示分段进度；向量索引仍用于语义检索
- **AI 阅读助手 Token 用量**：对话开始前显示预估消耗；结束后显示本次实际消耗（含 Agent 多轮与章节压缩），并写入助手消息 payload 供历史回看

修复：

- 关闭「行首缩进」后可正确恢复打开时的原始文本
- 短文本编码检测问题

## 2.1

改进：

- 流式加载 → 文本格式化 → 章节生成逻辑重构：
  - 加载期间不统计字数、不匹配章节，在加载完成后再统一处理，逻辑更简单清晰
  - 「压缩空行」「行首缩进」、章节匹配规则变更等，不再重新加载文件，直接基于内存中的物理文本进行操作，对大文件的体验有明显优化
- 切换「压缩空行」后恢复视口位置改为第二个视觉行
- 进入语音朗读时，从视口内第一行开始朗读（不再从中间行）
- 全文搜索：同一行多个匹配项现在会展示为多个搜索结果

## 2.0

新功能：

- **语音朗读**：支持 Edge TTS、系统语音和阿里云通义（DashScope / Qwen3-TTS）云端语音朗读
- **编辑模式**：方便对小说内容进行修改（理论上也可以用来写小说）
- **AI 阅读助手**：可以让 **AI** 帮忙分析剧情，或者回答小说的相关问题
- **角色卡**：借助 **AI** 检索小说中角色的相关信息生成摘要，通过**文生图**生成角色立绘
- 设置弹框改版：改为多标签页形式，以支持以上新功能的设置

改进：

- Monaco 中文化
- 书签列表：显示章节名，编辑书签支持「更新为当前行」
- 信息栏：
  - 点击文件路径弹出菜单（在文件管理器中显示、重新加载、关闭文件）
  - 点击编码弹出菜单（保存为 UTF-8、保存为 GB2312）

## 1.4

新功能：

- 侧栏改版
  - 标签页改为类 VS Code 的展示方式（图标 + 面板）
  - 增加「搜索」标签页，支持在当前文件内搜索
  - 顶栏的「高亮词」弹框移至侧栏作为单独的标签页
  - 显示「配色」和「设置」快捷入口
- 文件列表右键菜单增强：分类、移除、重命名、在新窗口中打开
- 设置面板
  - 增加「同步当前文件」开关，默认关闭，开启后，如果当前正在阅读的文件被修改，将自动重新加载
  - 增加「平滑滚动」开关，默认开启，关闭后，阅读区滚动不再使用平滑动画，以减缓长时间阅读的视觉疲劳
  - 增加「章节最少字数」设置，默认 100，少于这个字数将不会被识别为章节

改进：

- 文件列表：显示特定分类时，「清空」按钮改为「清空分类」，交互上更符合直觉
- 文件列表编辑模式：编辑过程只写内存不落盘，退出编辑模式时再统一写入
- 高亮词列表：点击高亮词的跳转走内联搜索逻辑，不再走查找栏逻辑
- 底栏：文件大小改为打开时获取，以免文件更新后大小不准确

## 1.3

新功能：

- 文件列表：支持文件分类、文件排序

改进：

- 拖放操作逻辑优化：拖放到文件列表为添加文件，拖放到文件列表以外为打开文件
- 文件列表编辑模式：选择文件的操作改为更贴近文件管理器的行为
- 模态弹框：右上角增加「关闭」按钮
- 打开查找栏时，自动激活书钉
- 高亮词列表：点击高亮词自动进入查找

修复：

- 加大字号快捷键 `Ctrl` + `+`：数字键盘的 `+` 现在可以正常触发了

## 1.2

新功能：

- 支持打开常见的电子书格式（`.epub`/`.mobi`/`.azw3`/`.fb2`/`.fbz`/`.pdf`/`.chm`）：打开时会转换为 `.txt` 进行加载

改进：

- 包体瘦身：
  - 剔除 Electron 的部分语言包（只保留简繁中文和英文语言包）
  - 渲染进程使用的第三方依赖直接打入主包不再 external
  - 启用 asar
- 设置面板增加「恢复默认」按钮，用于重置界面设置

## 1.1

构建相关：

- Windows：安装包支持自定义安装路径
- Windows：输出便携版（Portable）
- Linux：输出 AppImage 包

新功能：

- 支持自定义阅读区配色
- 支持自定义高亮词和高亮配色

改进：

- 引号/括号的高亮支持设置是否跨行匹配
- 字母高亮现在可以匹配到带声调的字符（如：`à`、`é`）
- 一些显示效果和交互体验的优化

修复：

- 窗口缩放导致恢复阅读进度出现误差
- 更多菜单显示的快捷键没有同步自定义的快捷键
